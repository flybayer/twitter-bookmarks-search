{
  "name": "next-web-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "patch-package",
    "start": "node next-dev-server.js",
    "start:content": "parcel watch content-script.js -d out",
    "start:firefox": "web-ext run -s out --url https://twitter.com/i/bookmarks --keep-profile-changes",
    "start:chrome": "web-ext run -s out -t chromium --url https://twitter.com/i/bookmarks --keep-profile-changes",
    "build": "yarn run build:next && yarn run build:content",
    "build:next": "next build && next export && mkdir out/assets && mv out/_next out/assets/",
    "build:content": "parcel build content-script.js -d out"
  },
  "browserslist": [
    "last 2 chrome versions"
  ],
  "dependencies": {
    "delay": "4.3.0",
    "match-sorter": "4.0.2",
    "next": "9.1.2",
    "patch-package": "6.2.0",
    "postinstall-postinstall": "2.0.0",
    "react": "16.11.0",
    "react-dom": "16.11.0",
    "react-tweet-embed": "1.2.2",
    "tweet-html": "1.4.0",
    "webextension-polyfill": "0.5.0"
  },
  "devDependencies": {
    "babel-core": "6.26.3",
    "babel-plugin-transform-class-properties": "6.24.1",
    "parcel-bundler": "1.12.4",
    "web-ext": "3.2.1"
  }
}
